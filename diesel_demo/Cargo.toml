[package]
name = "diesel_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "2.2.0", features = ["mysql"] }
mysqlclient-sys = { version = "0.4", features = ["bundled"] }
dotenvy = "0.15"
